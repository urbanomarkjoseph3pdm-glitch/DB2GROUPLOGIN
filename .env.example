# ============================================
# .env.example
# Kopya lang ito ng .env file OHNE totoong credentials.
# Ito ang isasama sa submission/repo — HINDI ang aktwal na .env mo.
# ============================================

MONGO_URI=mongodb+srv://<username>:<password>@<cluster-address>/<database-name>?retryWrites=true&w=majority
JWT_SECRET=replace_this_with_a_long_random_string
NODE_ENV=production
